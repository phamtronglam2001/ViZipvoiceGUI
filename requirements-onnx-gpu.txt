# Minimal GPU ONNX — chi wheel onnxruntime-gpu.
# Khong cai nvidia-* pip o day (tiet kiem ~1.5GB trung lap).
# Neu CUDA EP loi DLL: uv pip install -r requirements-onnx-gpu-cuda-libs.txt
onnxruntime-gpu>=1.18.0,<1.24
